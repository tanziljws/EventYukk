{
  "name": "ujikom-web-event",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd server && node server.js",
    "install:all": "npm install && cd frontend && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "mysql2": "^3.14.5",
    "node-cron": "^4.2.1",
    "xlsx": "^0.18.5"
  }
}
