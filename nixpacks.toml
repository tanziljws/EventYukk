[phases.setup]
nixPkgs = ['nodejs-20_x']

[phases.install]
cmds = [
  'npm ci',
  '(cd frontend && npm ci)',
  '(cd server && npm ci)'
]

[phases.build]
cmds = [
  'npm run build',
  'echo "Frontend build complete"'
]

[start]
cmd = 'cd server && NODE_ENV=production node server.js'
